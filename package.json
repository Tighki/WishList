{
  "name": "wishlist",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend --include=dev && npm run build --prefix frontend && node -e \"require('fs').rmSync('dist',{recursive:true,force:true});require('fs').cpSync('frontend/dist','dist',{recursive:true})\""
  },
  "engines": {
    "node": ">=20"
  }
}
